/**
 * Created by edeity on 17/11/2017.
 */

// 实例:用户单据
const CommonRegex  = {
    phone: /^1[34578]\d{9}$/,
    mail: /^[A-Za-zd]+([-_.][A-Za-zd]+)*@([A-Za-zd]+[-.])+[A-Za-zd]{2,5}$/
};

export default CommonRegex;